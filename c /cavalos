#include <stdio.h>

int main()
{
    int cavalos;
    
    printf("Insira a quantidade de cavalos: ");
    scanf("%d", &cavalos);
    printf("Você precisa de %d ferraduras.", cavalos * 4);

    return 0;
}
