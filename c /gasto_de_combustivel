#include <stdio.h>
 
int main() {
 
	int tmp, velocidade;
	double dist, litros;
	scanf("%d", &tmp);
	scanf("%d", &velocidade);

	dist = tmp * velocidade;
	litros = dist / 12.0;

	printf("%.3lf\n", litros);
 
    return 0;
}
